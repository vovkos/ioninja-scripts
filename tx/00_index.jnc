// infinite loop sending packets with ever-incrementing index
// this tx script can be used with any transport session

main ()
{
	for (size_t i = 0;; i++)
	{
		char const* packet = $"packet #$i\n";
		transmit (packet); // omitting length means, use strlen () 
		sys.sleep (1000);
	}
}
