// a simple loop which sends packets with ever-incrementing index

main ()
{
	for (size_t i = 0; i < 10; i++)
	{
		char const* packet = $"packet #$i\n";
		transmit (packet, sizeof (packet));
		sys.sleep (1000);
	}
}
